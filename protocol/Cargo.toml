[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[build-dependencies]
tonic-build = "0.12"

[dependencies]
anyhow = "1.0.100"
lazy_static = "1.5.0"
tonic = "0.12"
prost = "0.13"
serde = { version = "1.0", features = ["derive"] }
